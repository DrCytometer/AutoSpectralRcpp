% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{filter_contaminant_events_cpp}
\alias{filter_contaminant_events_cpp}
\title{Filter contaminant events by cosine similarity}
\usage{
filter_contaminant_events_cpp(event_mat, spectra_mat, threshold)
}
\arguments{
\item{event_mat}{Numeric matrix; events x detectors (the unstained
expression data, restricted to the spectral channels).}

\item{spectra_mat}{Numeric matrix; fluorophores x detectors (the reference
spectra, AF rows already removed, L-inf normalised).}

\item{threshold}{Numeric scalar.  Events with max cosine similarity
\eqn{\geq} \code{threshold} are flagged as contaminants.}
}
\value{
A logical vector of length \code{nrow(event_mat)}.  \code{TRUE}
means the event is clean (keep); \code{FALSE} means it is a likely
contaminant (remove).
}
\description{
For each event (row) in \code{event_mat}, computes cosine similarity
against every spectrum (row) in \code{spectra_mat}.  Returns a logical
vector that is \code{TRUE} for events whose maximum cosine similarity to
any spectrum is strictly below \code{threshold}.
}
