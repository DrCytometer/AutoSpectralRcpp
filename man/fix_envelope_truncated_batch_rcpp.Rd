% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{fix_envelope_truncated_batch_rcpp}
\alias{fix_envelope_truncated_batch_rcpp}
\title{Batched Truncated Spillover Slope, One Source Against Every Target (C++)}
\usage{
fix_envelope_truncated_batch_rcpp(
  x,
  Y,
  Threshold_target,
  threshold_source,
  start_slope,
  seed,
  max_coefficient = 0.2,
  max_mask_passes = 3L,
  mask_tolerance = 0.05,
  min_events = 200L,
  max_truncated_events = 20000L,
  k = 1.345,
  max_iter = 100L,
  tol = 1e-04,
  n_threads = 1L
)
}
\arguments{
\item{x}{Numeric vector, length \code{n}, the shared source abundance.}

\item{Y}{Numeric matrix, \verb{n x m}, one column per target.}

\item{Threshold_target}{Numeric matrix, \verb{n x m}, per-event per-target
positivity boundary.}

\item{threshold_source}{Numeric vector, length \code{n}, the source's own
per-event positivity boundary. Recycle to length \code{n} on the R side
before calling.}

\item{start_slope}{Numeric vector, length \code{m}, per-target warm starts.}

\item{seed}{Integer vector, length \code{m}, one RNG seed per target, drawn
from R's ambient RNG stream so a caller's \code{set.seed()} covers this
call.}

\item{max_coefficient}{double, largest accepted residual coefficient.
Default \code{0.2}.}

\item{max_mask_passes}{int, refinement passes after the initial fit.
Default \code{3}.}

\item{mask_tolerance}{double, relative change below which a pass is
treated as settled and refinement stops early. Default \code{0.05}.}

\item{min_events}{int, minimum selected events for a fit. Default \code{200}.}

\item{max_truncated_events}{int, cap on the events used for the robust
fit. Everything above the source threshold is kept and the negative
bulk below it is subsampled. Default \code{20000}.}

\item{k}{double, Huber tuning constant. Default \code{1.345}.}

\item{max_iter}{int, maximum IRLS iterations per fit. Default \code{100}.}

\item{tol}{double, IRLS relative coefficient-change tolerance. Default
\code{1e-4}.}

\item{n_threads}{int, OpenMP threads to split targets across. Default
\code{1} (serial). Every target's fit reads only its own column of \code{Y} and
\code{Threshold_target} and writes only its own output slot, so this is
embarrassingly parallel across targets -- but only raise it when this
call is not itself already running inside another parallel context
(\code{mclapply()} over samples, say). Oversubscribing cores that way is
the same failure mode \code{RhpcBLASctl::blas_set_num_threads(1)} already
guards against for BLAS inside forked workers elsewhere in this
package -- one source of truth for how many cores are in play at a
time, not two competing ones.}
}
\value{
A list with \code{slope} (numeric, length \code{m}, \code{NA} where the selected
population never reached \code{min_events}) and \code{n} (integer, length \code{m},
the final pass's selected event count, for \code{span}/diagnostic use back
in R).
}
\description{
Port of the truncated-estimator half of \code{.fix.envelope.slope()} --
\code{select.negative()} plus the \code{max.mask.passes} refinement loop plus
\code{.fix.huber.slope()} -- run for every target sharing one source's
abundance in a single call, instead of once per (source, target) pair.

Every working buffer (\code{w}, \code{resid}, the selection-index scratch space) is
allocated once, sized to \code{n}, and reused across every target and every
mask pass; only the \code{ni <= n} elements actually selected for a given
target's current pass are touched. This is the piece that a batched pure-R
implementation cannot get right no matter how it is vectorised: R's
\code{sweep()} / \code{outer()} return fresh objects every call, so a version built
from them reallocates matrices sized to the full working set on every one
of up to \verb{targets x mask.passes x irls.iterations} steps, several times
over. Batching only pays off once that allocation, and the two
\code{stats::median()} calls' R-level dispatch per iteration, are removed
entirely -- which needs compiled code, not a different R vectorisation.

Reproduces \code{select.negative()}'s bulk cap: every bright (source-positive)
event is kept regardless of file size, and the origin-hugging bulk below
the source threshold is subsampled down to \code{max_truncated_events} events
total once the negative-selected population exceeds it. The bulk sits at
the origin and carries no leverage on the fitted slope, but left uncapped
it sets the Huber M-estimator's iterative MAD-based scale almost
entirely once it outnumbers the bright tail by orders of magnitude --
diluting the tail's relative influence and biasing the fitted slope
toward zero. Each target gets its own \code{std::mt19937} stream, seeded from
\code{seed} and reused across passes (not reseeded per pass), so the sampling
is reproducible under R's \code{set.seed()} and identical regardless of
\code{n_threads} -- a target's subsample depends only on its own seed, never
on which thread happened to process it. Not bit-identical to
\code{.fix.envelope.slope()}, which draws its bulk subsample from a single
shared RNG stream advanced pair by pair, but statistically equivalent to
it.
}
