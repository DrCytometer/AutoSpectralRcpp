% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R, R/poisson_irls.R
\name{poisson_irls_rcpp_parallel}
\alias{poisson_irls_rcpp_parallel}
\title{Poisson IRLS Parallel}
\usage{
poisson_irls_rcpp_parallel(
  raw_data,
  spectra,
  beta_init,
  maxit = 25L,
  tol = 1e-06,
  n_threads = 1L,
  divergence_threshold = 10000,
  max_halving_steps = 20
)

poisson_irls_rcpp_parallel(
  raw_data,
  spectra,
  beta_init,
  maxit = 25L,
  tol = 1e-06,
  n_threads = 1L,
  divergence_threshold = 10000,
  max_halving_steps = 20
)
}
\arguments{
\item{raw_data}{matrix of cells x detectors}

\item{spectra}{matrix of fluorophores x detectors}

\item{beta_init}{initial unmixed matrix of cells x fluorophores}

\item{maxit}{maximum number of IRLS iterations}

\item{tol}{convergence tolerance}

\item{n_threads}{number of threads to use (OpenMP)}

\item{divergence_threshold}{maximum allowed divergence before fallback}

\item{max_halving_steps}{maximum number of halving steps to use to approach
convergence}

\item{raw_data_in}{matrix of cells x detectors}

\item{beta_init_in}{initial unmixed matrix of cells x fluorophores}

\item{noise_floor}{Numeric scalar or per-detector vector, default \code{125}.
Lower clamp on the denominator of the IRLS weight, preventing
\eqn{w_d \to \infty} for near-dark channels. Signal units, same
convention as \code{noise.floor} elsewhere in the package.}

\item{final_validation_ratio}{Numeric, default \code{1.5}. A cell's final
deviance is allowed to exceed its initial (WLS-seeded) deviance by at
most this multiple before the cell is reverted to \code{beta_init}.}
}
\value{
A list with \code{beta} (matrix of cells x fluorophores) and
\code{converged} (numeric vector, one entry per cell: \code{0} converged within
\code{tol}, \code{1} reverted mid-loop due to divergence, \code{2} reverted because the
final fit produced an invalid (near-zero or negative) predicted mean,
\code{3} reverted because final deviance exceeded \code{final_validation_ratio}
times initial deviance, \code{4} reached \code{maxit} without meeting \code{tol} but the
last accepted step was kept)
}
\description{
Poisson IRLS Parallel

Poisson IRLS Parallel
}
